[package]
name = "schema"
version = "0.1.0"
edition = "2024"

[features]
sqlx = ["dep:sqlx", "snowflake/sqlx", "mime/sqlx"]
serde = ["dep:serde", "chrono/serde", "snowflake/serde", "mime/serde"]

[dependencies]
# Workspace
mime.workspace = true
snowflake.workspace = true

# External
chrono.workspace = true
serde = { workspace = true, features = ["derive"], optional = true }
sqlx = { workspace = true, features = ["chrono"], optional = true }
