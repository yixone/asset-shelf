[package]
name = "instance"
version = "0.1.0"
edition = "2024"

[dependencies]
# Workspace
serde = { workspace = true, features = ["derive"] }
toml = { workspace = true, features = ["serde"] }
tracing = { workspace = true }
